const EMPTY = {
  port: 8080,
  cisUpdateUrl: "/",
  cisStatusUrl: "/",
  cisPersonApiUrl: "/",
  cisStatusTimeout: 10,
  cisStatusRetryCount: 3,
  personApiUrl: "/"
};

Object.freeze(EMPTY);

export { EMPTY };
